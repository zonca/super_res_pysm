Here we store the config files and parameters used for the `256_set3_b` simulation. We used the [SR3 model](https://github.com/javierhndev/Super-Resolution-SR3)  with commit `73cf4b4`

Before running the model, I created the data and run the *prepare* scripts.
